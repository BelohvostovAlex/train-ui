export declare enum AppPathes {
    MAIN = "/",
    MOVIE = "/movies/:id",
    BOOKING = "/bookings",
    PROFILE = "/profile/:id",
    NOT_FOUND = "/not-found"
}
export declare enum AppPathesWithoutSlug {
    MOVIE = "/movies/",
    PROFILE = "/profile/"
}
